---
// ===== Styles
import "./Link.styles.css";

// ===== Types
import type { LinkProps } from "./Link.types.ts";

// ===== Scripts
import { getLinkClassList } from "./Link.ts";

// ===== Props
const {
  as: Tag = "a",
  class: className,
  variant,
  isExternal = false,
  colorPalette,
  ...props
}: LinkProps = Astro.props;

const classList = getLinkClassList({ variant, colorPalette });
---

<Tag class:list={[classList, className]} rel={isExternal ? "noopener noreferrer" : undefined} {...props}>
  <slot />
</Tag>
