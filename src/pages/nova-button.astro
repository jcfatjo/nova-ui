---
// ===== Components
import BaseLayout from "../layouts/BaseLayout/BaseLayout.astro";
import Container from "../components/Layout/Container/Container.astro";
import Heading from "../components/Typography/Heading/Heading.astro";
import Button from "../components/Buttons/Button/Button.astro";
import Box from "../components/Layout/Box/Box.astro";
import Text from "../components/Typography/Text/Text.astro";

// ===== Props
const shapes = ["sharp", "rounded", "circular"];

const sizes = ["xs", "sm", "md", "lg", "xl"];

const fillings = ["solid", "tonal", "surface", "outline", "clear", "text"];

const colorPalettes = [
  "slate", "gray", "zinc", "neutral", "stone", "red", "orange", "amber", "yellow", "lime", "green",
  "emerald", "teal", "cyan", "sky", "blue", "indigo", "violet", "purple", "fuchsia", "pink", "rose",
];
---

<BaseLayout>
  <Container as="section" class="py-12 space-y-6">
    <Heading as="h1" size="2xl">Button</Heading>

    <Box>
      <Heading class="mb-2">Basic usage</Heading>
      <Box class="border border-gray-300 rounded-md p-8">
        <Button>Button</Button>
      </Box>
    </Box>

    <Box>
      <Heading class="mb-2">Shapes</Heading>
      <Box class="border border-gray-300 rounded-md p-8">
        <Box class="flex flex-row gap-6 items-center">
          {shapes.map((shape) => (
            <Button {shape}>
              {`${shape[0].toUpperCase()}${shape.slice(1)}`}
            </Button>
          ))}
        </Box>
      </Box>
    </Box>

    <Box>
      <Heading class="mb-2">Fillings</Heading>
      <Box class="border border-gray-300 rounded-md p-8">
        <Box class="flex flex-row gap-6">
          {fillings.map((fill) => (
            <Button {fill}>
              {`${fill[0].toUpperCase()}${fill.slice(1)}`}
            </Button>
          ))}
        </Box>
      </Box>
    </Box>

    <Box>
      <Heading class="mb-2">Sizes</Heading>
      <Box class="border border-gray-300 rounded-md p-8">
        <Box class="flex flex-row gap-6 items-center">
          {sizes.map((size) => (
            <Button size={size}>
              Button ({size})
            </Button>
          ))}
        </Box>
      </Box>
    </Box>

    <Box>
      <Heading class="mb-2">Icon</Heading>
      <Box class="border border-gray-300 rounded-md p-8">
        <Box class="flex flex-row gap-6">
          <Button colorPalette="teal" variant="solid">
            [] Email
          </Button>
          <Button colorPalette="teal" variant="outline">
            Call us []
          </Button>
        </Box>
      </Box>
    </Box>

    <Box>
      <Heading class="mb-2">Colors</Heading>
      <Box class="border border-gray-300 rounded-md p-8">
        <Box class="flex flex-col gap-6">
          {colorPalettes.map((colorPalette) => (
            <Box class="flex flex-row items-center gap-6">
              <Text class="min-w-[8ch]">
                {`${colorPalette[0].toUpperCase()}${colorPalette.slice(1)}`}
              </Text>
              <Button colorPalette={colorPalette}>
                Button
              </Button>
              <Button colorPalette={colorPalette} fill="outline">
                Button
              </Button>
              <Button colorPalette={colorPalette} fill="surface">
                Button
              </Button>
              <Button colorPalette={colorPalette} fill="tonal">
                Button
              </Button>
            </Box>
          ))}
        </Box>
      </Box>
    </Box>

    <Box>
      <Heading class="mb-2">Disabled</Heading>
      <Box class="border border-gray-300 rounded-md p-8">
        <Button disabled>
          Button
        </Button>
      </Box>
    </Box>

    <Box>
      <Heading class="mb-2">Loading</Heading>
      <Box class="border border-gray-300 rounded-md p-8">
        <Box class="flex flex-row gap-6">
          <Button loading>
            Click me
          </Button>
          <Button loading loadingText="Saving...">
            Click me
          </Button>
          <Button loading loadingText="Saving..." loadingSpinner="()">
            Click me
          </Button>
          <Button loading loadingText="Saving..." loadingSpinner="()" spinnerPlacement="end">
            Click me
          </Button>
        </Box>
      </Box>
    </Box>

    <Box>
      <Heading class="mb-2">Block</Heading>
      <Box class="border border-gray-300 rounded-md p-8 space-y-6">
        <Button block>
          Block solid button
        </Button>
        <Button fill="outline" block>
          Block outline button
        </Button>
        <Button fill="text" block>
          Block text button
        </Button>
      </Box>
    </Box>
  </Container>
</BaseLayout>
