---
// ===== Components
import BaseLayout from "../layouts/BaseLayout/BaseLayout.astro";
import Container from "../components/Layout/Container/Container.astro";
import Heading from "../components/Typography/Heading/Heading.astro";
import IconButton from "../components/Buttons/IconButton/IconButton.astro";
import Box from "../components/Layout/Box/Box.astro";
import Text from "../components/Typography/Text/Text.astro";

// ===== Props
const shapes = ["sharp", "rounded", "circular"];

const sizes = ["xs", "sm", "md", "lg", "xl"];

const fillings = ["solid", "tonal", "surface", "outline", "clear", "text"];

const colorPalettes = [
  "slate", "gray", "zinc", "neutral", "stone", "red", "orange", "amber", "yellow", "lime", "green",
  "emerald", "teal", "cyan", "sky", "blue", "indigo", "violet", "purple", "fuchsia", "pink", "rose",
];
---

<BaseLayout>
  <Container as="section" class="py-12 space-y-6">
    <Heading as="h1" size="2xl">Icon Button</Heading>

    <Box>
      <Heading class="mb-2">Basic usage</Heading>
      <Box class="border border-gray-300 rounded-md p-8">
        <IconButton aria-label="Search database">[]</IconButton>
      </Box>
    </Box>

    <Box>
      <Heading class="mb-2">Shapes</Heading>
      <Box class="border border-gray-300 rounded-md p-8">
        <Box class="flex flex-row gap-6 items-center">
          {shapes.map((shape) => (
            <Box class="flex flex-col items-center">
              <IconButton aria-label="Search database" {shape}>[]</IconButton>
              <Text>{`${shape[0].toUpperCase()}${shape.slice(1)}`}</Text>
            </Box>
          ))}
        </Box>
      </Box>
    </Box>

    <Box>
      <Heading class="mb-2">Fillings</Heading>
      <Box class="border border-gray-300 rounded-md p-8">
        <Box class="flex flex-row gap-6">
          {fillings.map((fill) => (
            <Box class="flex flex-col items-center">
              <IconButton aria-label="Call support" {fill}>[]</IconButton>
              <Text>{`${fill[0].toUpperCase()}${fill.slice(1)}`}</Text>
            </Box>
          ))}
        </Box>
      </Box>
    </Box>

    <Box>
      <Heading class="mb-2">Sizes</Heading>
      <Box class="border border-gray-300 rounded-md p-8">
        <Box class="flex flex-row gap-6 items-end">
          {sizes.map((size) => (
            <Box class="flex flex-col items-center">
              <IconButton aria-label="Search database" variant="outline" {size}>[]</IconButton>
              <Text>{size}</Text>
            </Box>
          ))}
        </Box>
      </Box>
    </Box>

    <Box>
      <Heading class="mb-2">Colors</Heading>
      <Box class="border border-gray-300 rounded-md p-8">
        <Box class="flex flex-row gap-6">
          {colorPalettes.map((colorPalette) => (
            <Box class="flex flex-col items-center">
              <IconButton aria-label="Call support" {colorPalette}>[]</IconButton>
              <Text>{`${colorPalette[0].toUpperCase()}${colorPalette.slice(1)}`}</Text>
            </Box>
          ))}
        </Box>
      </Box>
    </Box>
  </Container>
</BaseLayout>
